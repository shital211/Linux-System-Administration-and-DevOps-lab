*****Install and Configure Zabbix-Server and Agent on RHEL-9.8*****
On Server Site	

Step 1: ping google and redhat on VM
        #ping www.google.com
        #ping www.redhat.com

Step 2: Take Snapshot
        Name:-Before_install_zabbix_server

Step 3:- Set Hostname
         #hostnamectl set-hostname zabbixsrv.example.com

Step 4:- Mount CD/DVD to /mnt   
         #mount /dev/cdrom /mnt
         #yum clean all
         #yum repolist all

Note:-to enabled file based repo

Step 5:- Install Zabbix Repository
        #rpm -Uvh https://repo.zabbix.com/zabbix/7.0/rhel/9/x86_64/zabbix-release-latest.el9.noarch.rpm
		
Step 6:- Run this command to update your system’s repository:
        #dnf clean all
		
Step 7:- Install Zabbix Server, Frontend, and Agent
        #dnf install zabbix-server-mysql zabbix-sql-scripts zabbix-web-mysql zabbix-apache-conf zabbix-agent -y
		
Step 8:- Install and Configure MySQL Database
        #dnf  install  -y mysql-server
		#systemctl enable --now mysqld
		#mysql_secure_installation
		
Step 9:- Create a database and user for Zabbix: Run the following command to log into MySQL.
        #mysql -u root -p
		CREATE DATABASE zabbix CHARACTER SET utf8mb4 COLLATE utf8mb4_bin;
		CREATE USER 'zabbix'@'localhost' IDENTIFIED BY 'your_actual_password';
		GRANT ALL PRIVILEGES ON zabbix.* TO 'zabbix'@'localhost';
		FLUSH PRIVILEGES;
		SET GLOBAL log_bin_trust_function_creators = 1;
        EXIT;
		
Step 10:- Import Initial Schema and Data
        #zcat /usr/share/zabbix-sql-scripts/mysql/server.sql.gz | mysql -u zabbix -p zabbix
		
Step 11:- Configure Zabbix Server
        #vim /etc/zabbix/zabbix_server.conf
		
Update the following parameters:

DBHost=localhost
DBName=zabbix
DBUser=zabbix
DBPassword=your_actual_password
NodeAddress=localhost:10051

Step 12:- Start and Enable Apache to apply the changes:
        #systemctl enable --now httpd
		
Step 13:- Start Zabbix Server and Agent
        #systemctl enable --now zabbix-server zabbix-agent
		
Step 14:- Configure Firewall
        #firewall-cmd --permanent --add-port=10051/tcp
		#firewall-cmd --permanent --add-service=http
		#firewall-cmd --reload
		#firewall-cmd --list-all
		
	   
Step 15:- Access Zabbix Web Interface
Open a web browser and navigate to http://your_server_ip/zabbix.
Finally, log into the Zabbix frontend using the default credentials:
Username: Admin
Password: zabbix

=================================================================================================

Now,Install and Configure Zabbix-Agent on RHEL-9.8
Login to zabbix-agent (Or) Node
Step 1: ping google and redhat on VM
        #ping www.google.com
        #ping www.redhat.com

Step 2: Take Snapshot
        Name:-Before_install_zabbix_agent

Step 3:- 
Set Hostname
         #hostnamectl set-hostname node1.example.com

Step 4:- Mount CD/DVD to /mnt   
         #mount /dev/cdrom /mnt
         #yum clean all
         #yum repolist all

Note:-to enabled file based repo

Step 5:- Install Zabbix Repository
        #rpm -Uvh https://repo.zabbix.com/zabbix/7.0/rhel/9/x86_64/zabbix-release-latest-7.0.el9.noarch.rpm
		#dnf clean all
		
Step 6:- install zabbix-agent 
        #dnf install -y zabbix-agent
		
Step 7:- Configure Zabbix Server
        #vim  /etc/zabbix/zabbix_agentd.conf
		
Update the following parameters:
Server=<zabbix server ip>
ServerActive=<zabbix server ip>
Hostname=node1.example.com

Step 8:- restart the zabbix-agent service
        #systemctl restart zabbix-agent.service

Step 9:- Start Zabbix  Agent
        #systemctl enable --now  zabbix-agent
		
Step 10:- Configure Firewall
        #firewall-cmd --permanent --add-port=10050/tcp
		#firewall-cmd --reload
		#firewall-cmd --list-all
		
==============================================================================================

Now, How to sync zabbixserver to zabbix client via zabbix dashboard

Go back to zabbix dashboard

Steps:-
Click on Data collection 
inside Hosts Section 
create host
Host name:- node1.example.com
Visible name:- node1.example.com

Templates Select:- Linux by Zabbix agent
then click on select

Host groups:- Virtual machines
Interfaces:- Add-->Agent--->zabbixclient ip 

and then add 
wait for 2 min then your zabbix server and client is successfully sync
check Avaiability section ZBX is show in green colour

Now, Create dashboard for CPU Utilization,RAM Utilization, Disk Utilization
and Apache server status 

Go to Dashboard
create new dashboard
Owner:- Admin
name:- CPU Utilization
Add Widget
Type:- Gauge
Item:- select--->Host-->node1.example.com--->Cpu utilization

Owner:- Admin
name:- memory Utilization
Add Widget
Type:- Gauge
Item:- select--->Host-->node1.example.com--->Memory utilization

Owner:- Admin
name:- disk Utilization
Add Widget
Type:- Gauge
Item:- select--->Host-->node1.example.com--->disk utilization

Now create dashboard for apache service status 
Go to data collection  section inside template
click on new template 
name:- Apache service status 
visible name:- Apache service status 
templates:- linux by zabbix agent
template groups:- Templates/Operating systems

value mapping:- 
Name:- Apache Service Status 
Mappings 
type:- equals 
value:- 0
Mapped to :-Down

then add
type:- equals 
value:- 1
Mapped to :-Up

Add


Now to go to monitoring section
select Hosts
click on node1.example.com
click on Templates
choose by Apache service status 
and then update 

Now to go to monitoring section
select Hosts
click on node1.example.com
click on item 
name:- Apache Service Status 
type:- zabbix agent
Key:- proc.num[httpd]
Type of information:- Numeric (unsigned)
update interval:- 30s

Crete trigger
name/Even name:- Apache Service Status
severrity:- high
Expression:- Add--->node1.example.com-->apache server status
update

Now create dashboard 
Type:- Guage 
Name:- monitoting_apache
refresh interval:- 1 minute (default)
item :- Apache service status 
Min:- 0
Max:- 100
update
save dashboard

Now Trigger a CPU alert (for testing)

On the client (node1), run:

#dd if=/dev/zero of=/dev/null &
#top

This will push CPU usage high.

After 1–2 minutes, go back to your zabbix web dashboard →
you’ll see something like:

count is increase

Now down the apache web service  {zabbix client side}
#yum install -y httpd
#systemctl enable --now httpd
#systemctl status httpd 
#kill -9 <pid of httpd service>
#systemctl status httpd
go back to zabbix dashbord then monitor
apache service status 



Now to Set email alerts

tep 1:Enable 2-Step Verification

to set app password 
https://myaccount.google.com/apppasswords

Google Account → Security
App passwords
Name: Zabbix
Create
Google 16-character password generate


Now to zabbix dashboard
Go to Alerts
click on media types 
create new media types 
Name:- Zabbix_Alerts
Type:- Email
Email Provider:- Generic SMTP
SMTP server: smtp.gmail.com
SMTP port: 587
Email:- your personal email
Connection security:- STARTTLS
Authentication:- Username and Password
Username:- your personal email
Password:- apppasswords
Add

Now to add email to zabbix 
go to users 
click on Admin 
go to media--->then add 
Type:- Zabbix_Alerts
Send to :- your personal mail
Add

Now to  Alerts
inside media types 
choose onw email 
name:- Zabbix_Alerts and then test 
Send to:- your permanent email
and then test 

then you received test mail from zabbix in your personal email id 


Now to create trigger action
go to alerts 
inside Actions 
create 	Trigger actions
Name: Apache Email Alert
Conditions
Trigger severity ≥ High

Operations
Send message to: Admin
Send only to: Email
Save 

Now your email is configuration is completed
now to go zabbix client 
to down the httpd service
#systemctl stop httpd 

then you received zabbix alerts in zabbix bashboard and also 
received the mail from zabbix


****************************Thank You**********************************






