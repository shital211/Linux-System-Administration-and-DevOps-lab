aws_region = "ap-south-1"

# Replace with the latest Amazon Linux AMI ID
ami_id = "ami-xxxxxxxxxxxxxxxxx"

instance_type = "t3.micro"

instance_name = "Linux-EC2"

# Existing AWS Key Pair Name
key_name = "terraform-linux"

# Change this to your public IP for better security
allowed_ssh_cidr = "0.0.0.0/0"